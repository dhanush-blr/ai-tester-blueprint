# Fetch the JIRA ID and Create a Test Strategy Builder

Read the file of B.L.A.S.T.md again and my objective again, and create a lightweight React application which will take:

the Jira configuration
Jira email ID
Jira token
my Jira base URL
GROQ connection API details in the settings and take the JIRA ID and create the Test Strategy automatically. 
GROQ - openai/gpt-oss-120b (FREE) fETCH JIRA -> emAIL, token, JIRA